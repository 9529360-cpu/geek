# 极客订阅体系 Worker 配置
name = "geek-subscription"
main = "scripts/geek-subscription-worker.js"
compatibility_date = "2026-08-16"

# 管理后台子域名（顶层字段必须在 [[数组]] 之前）
routes = [
  { pattern = "admin.bbnba.com", custom_domain = true }
]

[[d1_databases]]
binding = "geek_subscriptions"
database_name = "geek-subscriptions"
database_id = "1e78a93a-36de-43db-88aa-4551f9991200"
