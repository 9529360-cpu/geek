name: actions-smoke

on:
  push:
    branches:
      - agent/audit-hardening-20260817

permissions:
  contents: read

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Verify runner
        run: |
          echo "GitHub Actions runner is working"
          node --version
