name: tmp-fix-518

on:
  push:
    branches:
      - fix/518-bridge-terminal-overflow

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v7
        with:
          ref: fix/518-bridge-terminal-overflow
          fetch-depth: 0
      - name: Runner probe
        run: echo BRIDGE_FIX_RUNNER_OK
