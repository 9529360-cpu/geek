{
  "name": "geek",
  "productName": "极客",
  "version": "1.2.18",
  "description": "极客——多平台多账号聊天客户端（WhatsApp / Telegram / LINE）",
  "main": "src/main-entry.cjs",
  "scripts": {
    "test": "node scripts/run-tests.cjs",
    "test:e2e": "node e2e/run.cjs",
    "start": "electron .",
    "electron:install": "install-electron --no",
    "pack": "npm run electron:install && npm run integrity:generate && electron-builder --config electron-builder.validation.yml --win --dir",
    "dist:test": "npm run electron:install && npm run integrity:generate && electron-builder --config electron-builder.validation.yml --win --publish never",
    "dist": "node scripts/release-build.cjs",
    "integrity:generate": "node scripts/generate-unpacked-integrity.cjs"
  },
  "devDependencies": {
    "@wdio/cli": "9.31.5",
    "@wdio/electron-service": "10.2.0",
    "@wdio/local-runner": "9.31.5",
    "@wdio/mocha-framework": "9.31.5",
    "electron": "^43.4.0",
    "electron-builder": "^26.15.3"
  },
  "dependencies": {
    "@wppconnect/wa-js": "^4.3.0",
    "electron-updater": "^6.8.9"
  },
  "allowScripts": {
    "electron@43.4.0": true
  }
}
