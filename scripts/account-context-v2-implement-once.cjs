'use strict';
const fs = require('node:fs');
const must = (cond, msg) => { if (!cond) throw new Error(msg); };
function replaceOnce(text, from, to, label) {
  must(text.includes(from), `missing anchor: ${label}`);
  must(text.indexOf(from) === text.lastIndexOf(from), `non-unique anchor: ${label}`);
  return text.replace(from, to);
}

// ui/index.html: instance settings is its own dialog; ordinary Settings is global-only.
{
  const p = 'ui/index.html';
  let s = fs.readFileSync(p, 'utf8');
  const proxyAnchor = '<!-- 独立代理IP 弹窗 -->';
  const accountDialog = `<!-- 实例账号设置：只属于被右击的具体实例，不复用全局 Settings -->\n<div id="account-settings-overlay" class="overlay hidden">\n  <div class="add-dialog" role="dialog" aria-modal="true" aria-label="账号设置">\n    <div class="settings-header">\n      <div class="settings-head-copy"><span>账号设置</span><small id="account-settings-target">—</small></div>\n      <button id="account-settings-close" class="settings-close" aria-label="关闭账号设置">×</button>\n    </div>\n    <div class="add-form">\n      <div class="row-item"><span>显示名</span><input type="text" id="account-settings-name" maxlength="80"></div>\n      <div class="row-item"><span>字体大小</span><input type="number" id="account-settings-fontSize" min="10" max="28" step="1"></div>\n      <div class="row-item"><span>字体颜色</span><input type="color" id="account-settings-fontColor"></div>\n      <div id="account-settings-status" class="settings-status" aria-live="polite"></div>\n    </div>\n    <div class="settings-footer">\n      <button id="account-settings-cancel" class="btn-plain">取消</button>\n      <button id="account-settings-save" class="btn-add">保存</button>\n    </div>\n  </div>\n</div>\n\n`;
  s = replaceOnce(s, proxyAnchor, accountDialog + proxyAnchor, 'proxy marker');
  s = replaceOnce(s,
`    <div class="settings-tabs">\n      <button class="settings-tab active" data-tab="global">应用设置</button>\n      <button class="settings-tab" data-tab="account">账号设置</button>\n    </div>\n\n`, '', 'settings tabs');
  const start = s.indexOf('    <div id="settings-account"');
  must(start >= 0, 'settings account start');
  const endMarker = '    <div class="settings-footer settings-footer--status">';
  const end = s.indexOf(endMarker, start);
  must(end > start, 'settings account end');
  s = s.slice(0, start) + s.slice(end);
  fs.writeFileSync(p, s);
}

// ui/settings-controller.js: global settings only; no account persistence ownership.
fs.writeFileSync('ui/settings-controller.js', `/* 极客普通设置体验层：只编排全局 config API；实例设置由右键上下文独立拥有。 */\n(() => {\n  'use strict';\n  const PROXY_GLOBAL_IDS = ['cfg-protocal','cfg-host','cfg-port','cfg-login','cfg-password'];\n  const APPEARANCE_DEFAULTS = Object.freeze({ theme: 'dark', accent: 'green' });\n  function create(deps = {}) {\n    for (const name of ['getConfig','setConfig','applyTheme']) if (typeof deps[name] !== 'function') throw new Error(\`设置面板缺少依赖: \${name}\`);\n    let bound = false, config = {}, previewSnapshot = null, statusTimer = null, returnFocus = null;\n    const el = id => document.getElementById(id);\n    const checked = id => !!el(id)?.checked;\n    const value = (id, fallback = '') => String(el(id)?.value ?? fallback);\n    const setChecked = (id, next) => { const n = el(id); if (n) n.checked = !!next; };\n    const setValue = (id, next) => { const n = el(id); if (n) n.value = String(next ?? ''); };\n    const overlayVisible = id => !!el(id) && !el(id).classList.contains('hidden');\n    function setStatus(text, kind = '') { const n = el('settings-status'); if (!n) return; n.textContent = text; n.dataset.state = kind; if (statusTimer) clearTimeout(statusTimer); if (kind === 'ok') statusTimer = setTimeout(() => { if (n.dataset.state === 'ok') { n.textContent = ''; n.dataset.state = ''; } }, 2200); }\n    function restoreFocus() { const t = returnFocus; returnFocus = null; if (t && t.isConnected !== false && typeof t.focus === 'function') t.focus(); }\n    function setProxyEnabled(enabled) { for (const id of PROXY_GLOBAL_IDS) { const n = el(id); if (n) n.disabled = !enabled; } el('settings-global-proxy-fields')?.classList.toggle('settings-fields-disabled', !enabled); }\n    function refreshProxyUi() { setProxyEnabled(checked('cfg-openProxy')); }\n    function fillGlobal() { setValue('cfg-theme', config.theme || 'dark'); setValue('cfg-accent', config.accent || 'green'); setChecked('cfg-autoLaunch', !!config.autoLaunch); setChecked('cfg-isStartupMinimize', !!config.isStartupMinimize); setChecked('cfg-messageSound', !!config.messageSound); setValue('cfg-lockPassword', config.lockPassword || ''); setChecked('cfg-openProxy', !!config.openProxy); setValue('cfg-protocal', config.protocal || 'http'); setValue('cfg-host', config.host || ''); setValue('cfg-port', config.port || ''); setValue('cfg-login', config.login || ''); setValue('cfg-password', config.password || ''); refreshProxyUi(); }\n    function validateProxy() { if (!checked('cfg-openProxy')) return null; const host = value('cfg-host').trim(), port = value('cfg-port').trim(); if (!host) return { message: '全局代理已启用，请填写代理主机', focusId: 'cfg-host' }; if (/\\s|:\\/\\//.test(host) || host.length > 253) return { message: '全局代理主机格式不正确，请只填写主机名或 IP', focusId: 'cfg-host' }; if (!/^\\d+$/.test(port) || Number(port) < 1 || Number(port) > 65535) return { message: '全局代理端口必须是 1-65535 的整数', focusId: 'cfg-port' }; return null; }\n    function validate() { const e = validateProxy(); return e ? { ok: false, ...e } : { ok: true }; }\n    function configPatch() { return { theme: value('cfg-theme','dark'), accent: value('cfg-accent','green'), autoLaunch: checked('cfg-autoLaunch'), isStartupMinimize: checked('cfg-isStartupMinimize'), messageSound: checked('cfg-messageSound'), lockPassword: value('cfg-lockPassword'), openProxy: checked('cfg-openProxy'), protocal: value('cfg-protocal','http'), host: value('cfg-host').trim(), port: value('cfg-port').trim(), login: value('cfg-login').trim(), password: value('cfg-password') }; }\n    async function load() { config = await deps.getConfig() || {}; fillGlobal(); setStatus(''); }\n    async function open() { if (overlayVisible('settings-overlay')) { el('settings-close')?.focus(); return; } const active = document.activeElement; returnFocus = active && typeof active.focus === 'function' ? active : null; const before = await deps.getConfig() || {}; previewSnapshot = { theme: before.theme || 'dark', accent: before.accent || 'green' }; await load(); el('settings-overlay')?.classList.remove('hidden'); el('settings-close')?.focus(); }\n    function close({ restorePreview = false } = {}) { if (!overlayVisible('settings-overlay')) return; if (restorePreview && previewSnapshot) deps.applyTheme(previewSnapshot.theme, previewSnapshot.accent); previewSnapshot = null; el('settings-overlay')?.classList.add('hidden'); setStatus(''); restoreFocus(); }\n    async function resetAppearance() { const ask = typeof deps.confirm === 'function' ? deps.confirm : window.confirm.bind(window); if (!ask('仅恢复主题和强调色为默认值？代理、锁屏和其他设置不会改变。')) return false; try { await deps.setConfig({ ...APPEARANCE_DEFAULTS }); config = { ...config, ...APPEARANCE_DEFAULTS }; fillGlobal(); deps.applyTheme(APPEARANCE_DEFAULTS.theme, APPEARANCE_DEFAULTS.accent); previewSnapshot = { ...APPEARANCE_DEFAULTS }; setStatus('外观已恢复默认 ✓','ok'); return true; } catch (e) { setStatus(\`恢复失败：\${String(e?.message || e).slice(0,120)}\`,'error'); return false; } }\n    async function save() { const v = validate(); if (!v.ok) { setStatus(v.message,'error'); el(v.focusId)?.focus(); return false; } const b = el('settings-save'); if (b) b.disabled = true; setStatus('正在保存…','working'); try { const next = configPatch(); await deps.setConfig(next); config = { ...config, ...next }; deps.applyTheme(next.theme,next.accent); previewSnapshot = { theme: next.theme, accent: next.accent }; if (typeof deps.afterSave === 'function') await deps.afterSave(); setStatus('已保存 ✓','ok'); return true; } catch (e) { setStatus(\`保存失败：\${String(e?.message || e).slice(0,120)}\`,'error'); return false; } finally { if (b) b.disabled = false; } }\n    function ensureResetButton() { if (el('settings-reset-appearance')) return; const host = el('cfg-theme')?.closest('.settings-card'); if (!host) return; const b = document.createElement('button'); b.type='button'; b.id='settings-reset-appearance'; b.className='btn-plain'; b.textContent='恢复外观默认'; host.appendChild(b); }\n    function handleKeydown(event) { if (event.defaultPrevented || event.repeat) return; const shortcut = (event.ctrlKey || event.metaKey) && !event.altKey && !event.shiftKey && event.key === ','; if (shortcut) { if (overlayVisible('lock-overlay')) return; event.preventDefault(); event.stopPropagation(); if (overlayVisible('settings-overlay')) return void el('settings-close')?.focus(); void open(); return; } if (event.key === 'Escape' && overlayVisible('settings-overlay')) { event.preventDefault(); event.stopImmediatePropagation(); close({ restorePreview: true }); } }\n    function bind() { if (bound) return; bound = true; el('settings-close')?.addEventListener('click', () => close({ restorePreview:true })); el('settings-cancel')?.addEventListener('click', () => close({ restorePreview:true })); el('settings-overlay')?.addEventListener('click', e => { if (e.target === el('settings-overlay')) close({ restorePreview:true }); }); ensureResetButton(); el('settings-reset-appearance')?.addEventListener('click', resetAppearance); el('cfg-openProxy')?.addEventListener('change', refreshProxyUi); for (const id of ['cfg-theme','cfg-accent']) el(id)?.addEventListener('change', () => deps.applyTheme(value('cfg-theme','dark'), value('cfg-accent','green'))); el('settings-save')?.addEventListener('click', save); document.addEventListener('keydown', handleKeydown); }\n    return Object.freeze({ bind, open, close, load, save, validate, refreshProxyUi, resetAppearance });\n  }\n  window.GeekSettingsController = Object.freeze({ create });\n})();\n`);

// ui/app.js: fixed-target instance settings + proxy; target-only refresh; global Settings remains global.
{
  const p = 'ui/app.js';
  let s = fs.readFileSync(p, 'utf8');
  const start = s.indexOf('  // ---------- 右键菜单（对齐原版：刷新应用/编辑应用/删除应用） ----------');
  const end = s.indexOf('  // ---------- 排序 ----------', start);
  must(start >= 0 && end > start, 'context block');
  const block = `  // ---------- 右键实例操作：唯一 owner，固定绑定被右击 account.id ----------\n  const ctxMenu = document.getElementById('ctx-menu');\n  async function refreshAccountInstance(accountId) {\n    const wv = wvMap.get(accountId);\n    if (wv) wv.reloadIgnoringCache();\n    try { const r = await window.api.accounts.list(); accounts = r?.accounts || r || []; renderSidebar(); renderTabs(); } catch {}\n  }\n  function showContextMenu(x, y, account) {\n    ctxMenu.innerHTML = \`<div class="ctx-item" data-act="refresh">刷新应用</div><div class="ctx-item" data-act="edit">账号设置</div><div class="ctx-item" data-act="proxy">代理设置</div><div class="ctx-item ctx-danger" data-act="delete">删除应用</div>\`;\n    const mw=150,mh=132; ctxMenu.style.left=Math.min(x,window.innerWidth-mw-8)+'px'; ctxMenu.style.top=Math.min(y,window.innerHeight-mh-8)+'px'; ctxMenu.dataset.accountId=account.id; ctxMenu.classList.remove('hidden');\n  }\n  function hideContextMenu(){ctxMenu.classList.add('hidden');}\n  ctxMenu.addEventListener('click', async e => { const item=e.target.closest('.ctx-item'); if(!item)return; const act=item.dataset.act, accountId=ctxMenu.dataset.accountId; hideContextMenu(); if(!accountId)return; const account=accounts.find(a=>a.id===accountId); if(act==='refresh') await refreshAccountInstance(accountId); else if(act==='edit'&&account) showAccountSettingsDialog(account); else if(act==='proxy'&&account) showProxyDialog(account); else if(act==='delete') removeAccount(accountId); });\n  document.addEventListener('click', hideContextMenu); window.addEventListener('blur', hideContextMenu);\n\n  const accountSettingsOverlay = document.getElementById('account-settings-overlay');\n  let accountSettingsAccountId = null;\n  function showAccountSettingsDialog(account) { accountSettingsAccountId = account.id; document.getElementById('account-settings-target').textContent = \`\${account.name || '未命名账号'} · \${familyOf(account.type).label}\`; document.getElementById('account-settings-name').value = account.name || ''; document.getElementById('account-settings-fontSize').value = account.fontSize || 16; document.getElementById('account-settings-fontColor').value = account.fontColor || '#18A058'; document.getElementById('account-settings-status').textContent=''; accountSettingsOverlay.classList.remove('hidden'); document.getElementById('account-settings-name').focus(); }\n  function closeAccountSettingsDialog(){ accountSettingsOverlay.classList.add('hidden'); accountSettingsAccountId=null; }\n  document.getElementById('account-settings-close').onclick=closeAccountSettingsDialog; document.getElementById('account-settings-cancel').onclick=closeAccountSettingsDialog; accountSettingsOverlay.onclick=e=>{if(e.target===accountSettingsOverlay)closeAccountSettingsDialog();};\n  document.getElementById('account-settings-save').onclick = async () => { if(!accountSettingsAccountId)return; const name=document.getElementById('account-settings-name').value.trim(); const fontSize=Number(document.getElementById('account-settings-fontSize').value); const status=document.getElementById('account-settings-status'); if(!name || name.length>80){status.textContent='显示名需要 1-80 个字符';return;} if(!Number.isInteger(fontSize)||fontSize<10||fontSize>28){status.textContent='字体大小必须是 10-28 的整数';return;} try { await window.api.accounts.update(accountSettingsAccountId,{name,fontSize,fontColor:document.getElementById('account-settings-fontColor').value}); await loadAccounts(); closeAccountSettingsDialog(); } catch(e){status.textContent='保存失败：'+String(e?.message||e).slice(0,100);} };\n\n  const proxyOverlay=document.getElementById('proxy-overlay'); let proxyAccountId=null;\n  function showProxyDialog(account){proxyAccountId=account.id; document.getElementById('proxy-openProxy').checked=!!account.openProxy; document.getElementById('proxy-protocal').value=account.protocal||'http'; document.getElementById('proxy-host').value=account.host||''; document.getElementById('proxy-port').value=account.port||''; document.getElementById('proxy-user').value=account.huser||''; document.getElementById('proxy-pwd').value=account.hpwd||''; proxyOverlay.classList.remove('hidden');}\n  function closeProxyDialog(){proxyOverlay.classList.add('hidden');proxyAccountId=null;} document.getElementById('proxy-close').onclick=closeProxyDialog; document.getElementById('proxy-cancel').onclick=closeProxyDialog; proxyOverlay.onclick=e=>{if(e.target===proxyOverlay)closeProxyDialog();};\n  document.getElementById('proxy-save').onclick=async()=>{if(!proxyAccountId)return; const enabled=document.getElementById('proxy-openProxy').checked, host=document.getElementById('proxy-host').value.trim(), port=document.getElementById('proxy-port').value.trim(); if(enabled&&(!host||!/^\\d+$/.test(port)||Number(port)<1||Number(port)>65535)){alert('请填写有效的代理主机和 1-65535 端口');return;} try{await window.api.accounts.update(proxyAccountId,{openProxy:enabled,protocal:document.getElementById('proxy-protocal').value,host,port,huser:document.getElementById('proxy-user').value.trim(),hpwd:document.getElementById('proxy-pwd').value}); await loadAccounts(); closeProxyDialog();}catch(e){alert('保存失败: '+e.message);}};\n\n`;
  s = s.slice(0,start) + block + s.slice(end);
  s = replaceOnce(s,
`  const settingsController = window.GeekSettingsController.create({\n    getConfig: () => window.api.config.get(),\n    setConfig: patch => window.api.config.set(patch),\n    getAccounts: () => window.api.accounts.list(),\n    updateAccount: (accountId, patch) => window.api.accounts.update(accountId, patch),\n    applyTheme,\n    getActiveId: () => activeId,\n    familyLabel: type => familyOf(type).label,\n    afterSave: async () => { await loadAccounts(); },\n  });\n  function openSettings() {\n    const preferred = accSelect.value || activeId || '';\n    return settingsController.open(preferred);\n  }`,
`  const settingsController = window.GeekSettingsController.create({\n    getConfig: () => window.api.config.get(),\n    setConfig: patch => window.api.config.set(patch),\n    applyTheme,\n    afterSave: async () => { await loadAccounts(); },\n  });\n  function openSettings() { return settingsController.open(); }`, 'settings controller init');
  fs.writeFileSync(p,s);
}

// ui/subscription.html: existing home is the single personal center; refresh fail-closed for display.
{
  const p='ui/subscription.html'; let s=fs.readFileSync(p,'utf8');
  const old = `      try {\n        const quota = await window.api.subscription.getQuota(true);\n        const remaining = quota.remaining_chars ?? 0;\n        if (quota.account_ref) $('#home-account-ref').textContent = '账号号 ' + quota.account_ref;\n        $('#home-icon').textContent = remaining > 0 ? '🎁' : '🔋';\n        $('#home-title').textContent = remaining > 0 ? '免费用户' : '字符已用完';\n        $('#home-quota').textContent = '剩余 ' + remaining.toLocaleString() + ' 字符';\n        $('#home-sub').textContent = remaining > 0\n          ? '注册赠送 2 万字符，用完可购买字符包'\n          : '购买字符包即可继续翻译，买断不限时';\n      } catch (e) {\n        $('#home-quota').textContent = '—';\n        $('#home-sub').textContent = '';\n      }`;
  const neu = `      try {\n        const refreshed = await window.api.subscription.refresh();\n        if (!refreshed?.loggedIn) { show('view-login'); return; }\n        if (refreshed.account_ref) $('#home-account-ref').textContent = '账号号 ' + refreshed.account_ref;\n        if (refreshed.email) $('#home-email').textContent = refreshed.email;\n        if (refreshed.networkError) {\n          $('#home-icon').textContent = '📡';\n          $('#home-title').textContent = '账户信息暂不可用';\n          $('#home-quota').textContent = '字符余额未知';\n          $('#home-sub').textContent = '网络连接失败，请稍后重新打开或刷新；不会用虚假余额代替。';\n        } else {\n          const remaining = refreshed.remaining_chars;\n          if (!Number.isSafeInteger(remaining) || remaining < 0) {\n            $('#home-icon').textContent = '📡'; $('#home-title').textContent = '账户信息暂不可用'; $('#home-quota').textContent = '字符余额未知'; $('#home-sub').textContent = '暂时无法确认字符余额，请稍后再试。';\n          } else {\n            $('#home-icon').textContent = remaining > 0 ? '🎁' : '🔋';\n            $('#home-title').textContent = remaining > 0 ? '免费用户' : '字符已用完';\n            $('#home-quota').textContent = '剩余 ' + remaining.toLocaleString() + ' 字符';\n            $('#home-sub').textContent = remaining > 0 ? '注册赠送 2 万字符，用完可购买字符包' : '购买字符包即可继续翻译，买断不限时';\n          }\n        }\n      } catch (e) { $('#home-icon').textContent='📡'; $('#home-title').textContent='账户信息暂不可用'; $('#home-quota').textContent='字符余额未知'; $('#home-sub').textContent='网络连接失败，请稍后再试。'; }`;
  s=replaceOnce(s,old,neu,'subscription quota display'); fs.writeFileSync(p,s);
}

// src/main.cjs: preserve narrow proxy fixes.
{
  const p='src/main.cjs'; let s=fs.readFileSync(p,'utf8');
  s=replaceOnce(s,"if (raw.protocal === 'https' || raw.protocal === 'socks4' || raw.protocal === 'socks5') {","if (raw.protocal === 'http' || raw.protocal === 'https' || raw.protocal === 'socks4' || raw.protocal === 'socks5') {",'account protocol');
  s=replaceOnce(s,'      account.openProxy ? account : null','      account.openProxy ? account : (configState.openProxy ? configState : null)','proxy fallback');
  fs.writeFileSync(p,s);
}

fs.unlinkSync('scripts/account-context-v2-implement-once.cjs');
fs.unlinkSync('.github/workflows/account-context-v2-implement-once.yml');
