# 极客-群组工具进度快照（2026-08-14 完整）

## 当前状态（代码已提交，git 可回滚）
- 群组工具 = 原版级 6 Tab（聊天设置/指令/输入链接/克隆/编辑/销毁）+ 原版 nav-pills + 底部账号栏；无操作价值的宣传说明横幅已移除，避免占用空间。
- 菜单：高级功能工具（群发消息/群组工具/群组链接/导出联系人/备份/数据报表）
- 群发：卡片式（保存消息/保存列表/排除/标签/Excel核验/多版本/@全体/定时）

## 最新视觉与克隆回归
- 克隆页主流程按原版整理为：邀请链接 → 克隆数量 → 克隆间隔；从当前已加入群组克隆作为折叠增强，不抢主流程。
- 链接克隆现在读取源群 ID、名称、简介、头像 URL、restrict/announce 权限；按 `群名 #N` 规则命名，复制资料/权限，按设置随机间隔执行，逐项统计失败，不再吞错后假报成功。
- 当前已选群组克隆也已统一到链接克隆的资料链路：复制简介、头像、restrict/announce 权限，按群名 `#N` 规则命名，使用克隆间隔，逐项统计失败。真实单群回归成功 1/失败 0，测试群参与者从 1 清理到 0。
- 原版执行态已补齐：逐项进度标签、`完成数/总数`、进度条和“停止后续克隆”。真实中止回归使用 2 个目标、8 秒间隔：第一个完成后显示 1/2、50%，点击停止后最终成功 1/失败 0，第二个未创建；测试群已清理。
- 权限根因已修复：旧代码把 `announce` 当成属性名、并把字符串传给底层 `setGroupProperty`，实际报 `toJid is not a function`；现统一使用 `W.group.setProperty(gid, 'announcement'|'restrict', boolean)`。编辑页文案与语义已校正，并增加“全体成员发消息/编辑资料”的可逆选项。
- 权限真实回归：`announcement false→true→false`、`restrict false→true→false` 均通过；再将源群临时设为双 true，选中群组克隆后新群实际 `restrict=true/announce=true`，随后源群恢复双 false、克隆群参与者清理至 0。
- 头像根因已修复：`sendSetPicture` 不接受 `File`，必须按原版先中心裁剪为 `96×96` 与 `640×640` 两张 JPEG data URL，再传入 Wid。桌面真实图片 `桌面照片 副本.jpg` 已完成 UI 文件选择→保存头像→头像 URL 验证；选中群组克隆和邀请链接克隆的新群均实测 `avatar=true`。两个克隆群参与者均清理到 0，源测试群头像缓存刷新后恢复为 null，桌面原图未改动。
- 成员/管理员旧代码已替换：不再使用 `WAWebGroupsParticipantsApi` 并吞错，改用 `W.group.addParticipants/promoteParticipants/demoteParticipants/removeParticipants`；逐号码读取 code/message，隐私限制返回邀请链接时明确标记“需邀请”，只有直接加入/已存在的成员才提升管理员。
- 管理员真实回归：编辑页输入现有测试成员 LID 并勾选“设为管理员”，UI 返回“设管理员1人”，metadata `admin=true/count=2`；随后 `demoteParticipants` 恢复为 `admin=false/count=2`。新增成员从 1→2 的独立测试因服务器 `rate-overlimit` 暂未强行执行，不能标记为端到端通过。
- 退出/销毁已改为 WPP 高层 API：销毁只移除非当前账号成员，再 `W.group.leave`，并以 metadata `participants=0` 验证；UI 退出和销毁分别用旧单人测试群回归通过。另修复操作后 `gtSelected` 未清理导致下一次重复处理旧群的问题。
- 所有批量群操作已补可配置随机间隔，统一硬下限 5 秒、默认 5–10 秒：输入链接加入、批量获取群链接、链接克隆、已选群克隆、批量编辑、批量销毁和批量退出。代码只在相邻任务之间等待，最后一个结束后不空等；状态栏显示已处理数量和本次等待秒数。因第二账号风控状态，本轮只做语法/结构/分支静态验证，没有再次触发真实群操作。

## 本次全部真实验证通过 ✅
| 功能 | 状态 | 说明 |
|---|---|---|
| 群组工具模块激活 | ✅ | 根因：app.js 找 `gt-groups`，UI 是 `gt-group-list` → 整个 JS 块从未初始化（用户指出"只有 UI 没功能"的真因） |
| 克隆（选择群） | ✅ | WPP.group.create(name, [成员Wid数组])——之前传 me 字符串参数错误导致 undefined |
| 链接克隆（原版方式） | ✅ | 粘贴群链接 → 读取源群 ID/名称/简介/头像/权限 → 按 `群名 #N` 创建 → 复制资料和权限 → 按区间间隔执行；真实回归成功 1/失败 0，测试群已清理 |
| 获取群链接 | ✅ | WPP.group.getInviteCode 封装 iAmAdmin+queryGroupInviteCode(wid, isAdmin)——之前直接调 queryGroupInviteCode(wid) 缺第二个参数崩 "reading iAmAdmin" |
| 退出/销毁群组 | ✅ | `W.group.leave` 已多次实测使 metadata participants 变 0；销毁先用 `W.group.removeParticipants` 移除非自己的成员，再退出并验证 remaining=0。旧 WAP stanza 保留为历史研究，不再作为正式路径。 |
| 编辑（名称/简介） | ✅ | WAWebGroupModifyInfoJob.setGroupSubject/setGroupDescription（Wid 实例参数） |
| 指令监听 | ✅ | 对齐原版：自定义指令名 ban/adm/deadm/infog/tagall（localStorage 原版键）+ 引用消息或 @参数触发 + permitiradmins 权限检查 + apagarcomando 撤回指令消息；infog 群信息格式同原版；tagall 隐藏字符(String.fromCharCode(8206).repeat(4001))技巧 |
| 自动加群 | ✅ | chat.new_message 匹配 chat.whatsapp.com 链接 → W.group.join（实测 JOINED） |
| 自动删群 | ✅(机制) | gp2 消息监听（subtype remove/leave + recipients 含自己 → W.chat.delete）——真实触发需第二账号验证；group.participant_changed 事件在旧版页面不触发（wa-js wrap updateDBForGroupAction 不兼容） |
| 配置同步 | ✅ | UI 保存 → webview localStorage（__gtAutoCfg + 原版键 comandoban 等） |

## 关键 API 结论
- 正式退出路径：`await W.group.leave(gid)`，随后读取 `GroupMetadataStore/WAWebGroupMetadataCollection`，只有 `participants.length === 0` 才报告成功。
- 正式销毁路径：读取当前账号 LID，`W.group.removeParticipants(gid, 非自己成员)` 后再 `W.group.leave(gid)`；不移除自己，不吞错。
- `WAWebGroupModifyInfoJob.setGroupSubject/Description` 需 Wid 实例（chat.id 对象，不是字符串）
- `WAWebCreateGroupAction.createGroup` 参数形态 = ({title,...}, 成员Wid数组)——WPP.group.create 封装正确
- `UserPrefs.getMeUser()` 返回 Wid 对象（{server,user,_serialized}），消息 sender 是 lid 格式——指令权限判断用 getMaybeMeLidUser（原版 mEandMeLid 比较）
- `GroupMetadataStore.find()` 返回 Model 本身（不是 {groupMetadata} 包装）

## 监听器架构
- GT_AGENT_SOURCE 注入 WA webview（dom-ready 自动 + loadGtGroups 时确保 + window.__gtReinjectAll 强制重注入）
- 版本化（__gtAgentVersion）+ 可卸载（__gtUninstall 移除旧监听）——升级/开发不残留
- 配置存 webview 域 localStorage（与外壳 localStorage 不互通，syncGtCfgToWebview 同步）
- 当前版本 v9（正式版，无调试日志）

## 测试工具
- test/cdp-eval.cjs：CDP 在 renderer 执行 JS（返回 byValue）
- test/cdp-reload.cjs：Page.reload ignoreCache 强制刷新（file:// 缓存坑）
- test/fix_escapes.py：修复 patch 工具引入的 4 反斜杠双倍转义（patch 对反斜杠有 JSON 转义 bug，改完跑它）

## 注意
- 重启命令：cd "D:/项目/whatsapp-multi-phase1" && npx electron . --remote-debugging-port=9344
- 改 app.js 后必须 Page.reload ignoreCache（普通 reload 会吃到 file:// 缓存旧脚本）
- 测试群随便用（阿豪测试号）；真实群别乱退
