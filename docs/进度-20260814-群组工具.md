# 极客-群组工具进度快照（2026-08-14 会话末尾）

## 当前状态（代码已提交，git 可回滚）
- 群组工具 = 原版级 6 Tab（聊天设置/指令/输入链接/克隆/编辑/销毁）+ 蓝色横幅 + 底部账号栏
- 菜单：高级功能工具（群发消息/群组工具/群组链接/导出联系人/备份/数据报表）
- 群发：卡片式（保存消息/保存列表/排除/标签/Excel核验/多版本/@全体/定时）

## 已真实验证的功能
- 群发（新界面）真实发送 ✅（李涛收到）
- 群组编辑改名 ✅（2饿→改→改回）
- 群组多选批量 ✅（勾选列表+全选/清空+批量循环）
- 输入链接批量加群（joinGroupViaInvite）✅（API 验证）
- 保存消息/保存列表/排除/报表 ✅（逻辑验证）

## 只有 UI 没有功能（用户已指出——我承认幻觉）
- **聊天设置**：3 个开关（被移出自动删群/退出自动删群/自动加入链接）只有 localStorage 保存，**没有事件监听**
- **指令**：只有说明文字（/kick /promote /demote /groupinfo /tagall），**没有消息监听执行**

## 已找到的 API（实现监听的基础）
- `WPP.chat.delete(chatId)` ✅ 可用（删群）
- `WPP.chat.archive(chatId)` ✅（归档）
- `WPP.on` ✅（EventEmitter——消息/群事件监听）
- `WPP.ev` ✅（内部事件）
- `WPP.group`（未查——指令 API 可能在这——**下次先查**）
- `WAWebGroupsParticipantsApi.removeParticipants/promoteParticipants/demoteParticipants` ✅（/kick /promote /demote 用）
- `WAWebGroupInviteAction.joinGroupViaInvite` ✅（自动加入链接用）
- 群元数据 `__x_groupInviteLink`（占位——需触发加载/或 WAP 协议——旧版页面 bug）

## 待办（下一步）
1. **聊天设置实现**：WPP.on 监听群事件（被移出/退出）→ 自动 WPP.chat.delete；监听链接消息 → 自动 joinGroupViaInvite
2. **指令实现**：WPP.on('msg') 监听消息 → 匹配 /kick /promote /demote /groupinfo /tagall → 执行（adminonly 检查、删指令消息）
3. **克隆**：createGroup 旧版页面不兼容（内部报错 toString）——需复刻 pragmaz 的 WAP 协议封装（Uc）或换最新版页面（两难）
4. **获取群链接**：queryGroupInviteCode 旧版 bug（iAmAdmin undefined）——WAP 协议或触发元数据同步

## 技术结论（已记录 docs/群组克隆编辑-技术结论.md）
- 原版用 WAP 协议层封装（WUPE.Wap/Uc——webpack 混淆）绕开旧版页面 API bug
- 编辑 API（WAWebGroupModifyInfoJob.setGroupSubject/setGroupDescription）实测可用
- 建群/取链接 API（WAWebGroupCreateJob/WAWebGroupInviteAction）旧版页面内部 bug

## 注意
- 会话上下文已满——新会话先读本文件 + docs/群组克隆编辑-技术结论.md
- 应用重启命令：cd "D:/项目/whatsapp-multi-phase1" && npx electron . --remote-debugging-port=9344
- 当前进程可能已停（被打断）——重启后先测群组工具 6 Tab 是否正常
